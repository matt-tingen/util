{
  "name": "@matt-tingen/util",
  "version": "0.1.0",
  "repository": "git@github.com:matt-tingen/util.git",
  "author": "Matt Tingen <matt.tingen@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc -p config/dist.json",
    "build-test": "yarn clean && tsc -p config/dist.json",
    "test": "yarn build-test && ava"
  },
  "dependencies": {
    "ava": "^0.25.0",
    "typescript": "^2.8.1"
  }
}
